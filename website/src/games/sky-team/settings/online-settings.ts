/**
 * What an online landing needs before it starts.
 *
 * @module
 * @remarks
 * Nothing at all. The table is always two, the scenario is always Montreal, and
 * there is no variant to agree on - so this exists only so the shared online
 * layer has the shape it expects.
 */

/** How long a seat may think before the computer takes over, in ms. */
export const AUTO_PLAY_MS = 90_000;
